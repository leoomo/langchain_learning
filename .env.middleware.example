# AgentLoggingMiddleware 环境变量配置示例
# 复制此文件为 .env 并根据需要修改配置

# 日志级别 (DEBUG, INFO, WARNING, ERROR, CRITICAL)
AGENT_LOG_LEVEL=INFO

# 日志输出方式
AGENT_LOG_CONSOLE=true          # 控制台输出 (true/false)
AGENT_LOG_FILE=false            # 文件输出 (true/false)

# 日志文件路径 (当 AGENT_LOG_FILE=true 时生效)
AGENT_LOG_FILE_PATH=logs/agent.log

# 性能监控
AGENT_PERF_MONITOR=true         # 启用性能监控 (true/false)

# 工具调用追踪
AGENT_TOOL_TRACKING=true        # 启用工具调用追踪 (true/false)

# 敏感信息过滤
AGENT_SENSITIVE_FILTER=true     # 启用敏感数据过滤 (true/false)

# 日志格式 (可选，使用默认格式)
# AGENT_LOG_FORMAT=[{timestamp}] {level} | {session_id} | {component}: {message}

# 最大日志长度 (防止日志过长，单位：字符)
AGENT_MAX_LOG_LENGTH=2000

# 会话ID生成方式 (uuid, timestamp)
AGENT_SESSION_ID_GEN=uuid

# 示例配置：
# 开发环境 - 详细日志，控制台输出
# AGENT_LOG_LEVEL=DEBUG
# AGENT_LOG_CONSOLE=true
# AGENT_LOG_FILE=false
# AGENT_PERF_MONITOR=true

# 生产环境 - INFO级别，文件输出
# AGENT_LOG_LEVEL=INFO
# AGENT_LOG_CONSOLE=false
# AGENT_LOG_FILE=true
# AGENT_LOG_FILE_PATH=logs/production.log
# AGENT_PERF_MONITOR=false